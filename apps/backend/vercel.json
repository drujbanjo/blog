{
	"version": 2,
	"builds": [
		{
			"src": "apps/backend/src/main.ts",
			"use": "@vercel/node",
			"config": { "maxLambdaSize": "50mb" }
		}
	],
	"routes": [{ "src": "/(.*)", "dest": "apps/backend/src/main.ts" }]
}
